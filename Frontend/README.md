# Frontend (Vite + React)

## Environment
- Copy `Frontend/.env.example` to `Frontend/.env` if you want to change the API URL.
- `VITE_API_BASE_URL` defaults to `http://localhost:8000`.

## App Modes
- Notes: `/dashboard`
- Writer: `/write`
